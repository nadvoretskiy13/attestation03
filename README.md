### Registry — hospital registry demo

Run with Docker:

1. docker compose build
2. docker compose up

Web:
http://localhost:8080/

Swagger UI:
http://localhost:8080/swagger-ui.html

Database:
Postgres 15 (user/pass, DB hospital)